import { type AppProps } from "contexts/process/types";

const config: AppProps = {
  backgroundColor: "transparent",
  defaultSize: {
    height: 500,
    width: 400,
  },
  icon: "/System/Icons/user.webp",
  resizable: false,
  title: "Login",
};

export default config;
